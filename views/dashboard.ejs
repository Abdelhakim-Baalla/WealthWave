<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <script src="https://cdn.jsdelivr.net/npm/chart.js@4.5.0/dist/chart.umd.min.js"></script>
  </head>
  <body>
    <% if (typeof error !== 'undefined' && error) { %>
    <div class="error-message"><%= error %></div>
    <% } %> <% if (typeof message !== 'undefined' && message) { %>
    <div class="message-message"><%= message %></div>
    <% } %>

    <h1>Bonjour <%= utilisateur.prenom %> <%= utilisateur.nom %></h1>
    <a href="/">Acceuil</a><br />
    <a href="/deconnexion">Deconnexion</a><br />
    <a href="/profile">Profile</a><br />
    <a href="/ajouter-transaction">Ajouter Transaction</a><br />
    <a href="/transactions">Transactions</a><br />
    <a href="/categorie/ajouter">Ajouter Nouvelle Categorie</a><br />
    <a href="/categories">Categories</a><br />
    <a href="/budgets">Budgets</a><br />
    <a href="/objectifs">Objectifs</a><br />

    <hr />

    <h2>Résumé des Transactions</h2>
    <p>Total des Revenus: <%= totalRevenus %> <%= utilisateur.devise %></p>
    <p>Total des Frais: <%= totalFrais %> <%= utilisateur.devise %></p>
    <p>Solde Net: <%= soldeNet %> <%= utilisateur.devise %></p>

    <hr />
    <h2>Somme des Transactions Par Catégorie</h2>
    <table border="1">
      <thead>
        <tr>
          <th>Catégorie</th>
          <th>Somme</th>
        </tr>
      </thead>
      <tbody>
        <% let total = 0; Object.entries(sommeParCategorie).forEach(([categorie,
        somme]) => { total+=somme; %>
        <tr>
          <td><%= categorie %></td>
          <td><%= somme %> <%= utilisateur.devise %></td>
        </tr>
        <% }) %>
        <tr>
          <td><strong>Total</strong></td>
          <td><strong><%= total %> <%= utilisateur.devise %></strong></td>
        </tr>
      </tbody>
    </table>

    <% let pourcentageRevenus = (totalRevenus / (totalRevenus + totalFrais) *
    100) || 0; let pourcentageFrais = (totalFrais / (totalRevenus + totalFrais)
    * 100) || 0; %>

    <h2>Progressions</h2>

    <span>Revenus</span>
    <div
      style="
        border: 1px solid #ccc;
        width: 100%;
        height: 30px;
        margin-bottom: 10px;
      "
    >
      <div
        data-width="<%= Math.floor(pourcentageRevenus) %>"
        style="
          height: 100%;
          background-color: green;
          text-align: right;
          color: white;
        "
      >
        <%= Math.floor(pourcentageRevenus) %>%
      </div>
    </div>

    <span>Frais</span>
    <div style="border: 1px solid #ccc; width: 100%; height: 30px">
      <div
        data-width="<%= Math.floor(pourcentageFrais) %>"
        style="
          height: 100%;
          background-color: red;
          text-align: right;
          color: white;
        "
      >
        <%= Math.floor(pourcentageFrais) %>%
      </div>
    </div>

    <h2>Dépenses par Catégorie</h2>
    <% if ((Object.keys(depensesParCategorie || {})).length === 0) { %>
    <p>Aucune dépense à afficher pour le moment.</p>
    <% } else { %> <% const _labelsJson =
    JSON.stringify(Object.keys(depensesParCategorie || {})); %> <% const
    _valuesJson = JSON.stringify(Object.values(depensesParCategorie || {})); %>
    <canvas
      id="categorieChart"
      width="200"
      height="200"
      data-labels="<%= _labelsJson %>"
      data-values="<%= _valuesJson %>"
    ></canvas>
    <script>
      const categorieEl = document.getElementById("categorieChart");
      const labelsCategorie = JSON.parse(categorieEl.dataset.labels || "[]");
      const dataCategorie = JSON.parse(categorieEl.dataset.values || "[]");
      const ctxCategorie = categorieEl.getContext("2d");
      new Chart(ctxCategorie, {
        type: "pie",
        data: {
          labels: labelsCategorie,
          datasets: [
            {
              data: dataCategorie,
              backgroundColor: [
                "#ef4444",
                "#3b82f6",
                "#10b981",
                "#f59e0b",
                "#8b5cf6",
                "#06b6d4",
                "#f97316",
                "#22c55e",
              ],
            },
          ],
        },
        options: {
          plugins: {
            title: { display: true, text: "Dépenses par Catégorie" },
            legend: { position: "bottom" },
          },
        },
      });
    </script>
    <% } %>
    <script>
      (function () {
        var bars = document.querySelectorAll("[data-width]");
        for (var i = 0; i < bars.length; i++) {
          var value = parseInt(bars[i].getAttribute("data-width") || "0", 10);
          if (!isNaN(value)) {
            bars[i].style.width = value + "%";
          }
        }
      })();
    </script>
  </body>
</html>
