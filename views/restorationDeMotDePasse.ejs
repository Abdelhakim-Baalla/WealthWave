<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Restoration de mot de passe</title>
  </head>
  <body>
    <% if (typeof error !== 'undefined' && error) { %>
    <div class="error-message"><%= error %></div>
    <% } %> <% if (typeof message !== 'undefined' && message) { %>
    <div class="message-message"><%= message %></div>
    <% } %>
    <h1>Restoration de mot de passe</h1>
    <form
      action="/restorer-mot-de-passe?token=<%= token %>&email=<%= newEmail %>"
      method="post"
    >
      <label for="newPassword">Nouveaux mot de passe</label>
      <input type="password" name="newPassword" id="newPassword" required />
      <button type="submit">Réinitialiser le mot de passe</button>
    </form>
    <% if (typeof resend !== 'undefined' && resend) { %>
    <form action="/motdepasseoublie" method="post">
      <input type="hidden" name="email" value="<%= newEmail %>" />
      <button type="submit">Renvoyer l'email de réinitialisation</button>
    </form>
    <% } %>
  </body>
</html>
