<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <% if (typeof error !== 'undefined' && error) { %>
    <div class="error-message"><%= error %></div>
    <% } %> <% if (typeof message !== 'undefined' && message) { %>
    <div class="message-message"><%= message %></div>
    <% } %>

    <form action="/categorie/modifier" method="post">

      <label for="nom">Nom de categorie</label>
      <input type="text" name="nom" id="nom" value="<%= typeof categorie.nom !== 'undefined' ? categorie.nom : '' %>" /><br />
      <hr />
      <input type="hidden" name="id" value="<%= categorie.id %>">
      <button type="submit">Crée Categorie</button>
    </form>

    <a href="/transactions">Retourner Au transactions</a>
    <a href="/categories">Retourner Au categories</a>
  </body>
</html>
